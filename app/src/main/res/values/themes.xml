<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.YogaApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary and background colors -->
        <item name="android:statusBarColor">@color/background_color</item>
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/accent_color</item>
        <item name="colorAccent">@color/accent_color</item>
        <item name="android:colorBackground">@color/accent_color</item>
        <!-- Button and text styling -->
        <item name="android:textColor">@color/text_color</item>
        <item name="android:buttonStyle">@style/ButtonStyle</item>
    </style>
    <!-- Custom button style -->
    <style name="ButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/button_text_color</item>
    </style>
    <style name="RoundedCalendarView">
        <item name="android:background">@drawable/rounded_calendar_background</item>
        <item name="android:padding">16dp</item>
    </style>
</resources>
